ucenter分支:cart

```sql
CREATE TABLE `mall_cart_goods_device` (
  `id` int(10) unsigned NOT NULL AUTO_INCREMENT,
  `udid` int(10) unsigned NOT NULL COMMENT 'udid',
  `goods_id` int(10) unsigned NOT NULL COMMENT '商品ID',
  `goods_group_id` int(10) unsigned NOT NULL COMMENT '商品组ID',
  `quantity` int(10) unsigned NOT NULL COMMENT '购买数量',
  `price` int(10) unsigned NOT NULL COMMENT '购买单价',
  `discount` int(11) NOT NULL DEFAULT '0' COMMENT '折扣',
  `create_time` bigint(20) NOT NULL,
  `update_time` bigint(20) NOT NULL,
  `state` enum('selected','unselected') DEFAULT 'selected',
  PRIMARY KEY (`id`),
  UNIQUE KEY `u_key` (`udid`,`goods_id`),
  KEY `i_udid` (`udid`,`create_time`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8 COMMENT='购物车商品表(未登录的)';

CREATE TABLE `mall_pay_order` (
  `id` bigint(13) unsigned NOT NULL AUTO_INCREMENT,
  `total_amount` int(11) DEFAULT NULL COMMENT '总金额',
  `state` tinyint(3) DEFAULT NULL COMMENT '1-待支付, 2-已支付，3-取消, 4-过期',
  `user_id` int(11) DEFAULT NULL,
  `create_time` bigint(13) DEFAULT NULL,
  `pay_method` varchar(20) DEFAULT NULL,
  `pay_time` bigint(13) DEFAULT NULL,
  `trans_id` varchar(45) DEFAULT NULL,
  `state_desc` varchar(60) DEFAULT NULL,
  `cancel_type` tinyint(3) DEFAULT NULL,
  `cancel_time` bigint(13) DEFAULT NULL,
  `cs_phone` varchar(255) DEFAULT NULL COMMENT '客服电话',
  `cs_wechat` varchar(255) DEFAULT NULL COMMENT '客服微信号',
  `cs_email` varchar(255) DEFAULT NULL COMMENT '客服邮箱',
  PRIMARY KEY (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8;


alter table mall_cart_goods MODIFY column `state` enum('selected','unselected') DEFAULT 'selected';

alter table mall_cart_goods_archive MODIFY column `id` int(10) unsigned NOT NULL AUTO_INCREMENT;

alter table `mall_order_detail` add column `goods_using` int(11) NOT NULL DEFAULT '0' COMMENT '正在使用数量';

alter table `mall_order_goods` add column `image_url` varchar(200) DEFAULT NULL;

```

